# A3b 用:单节点图,主备都配置成只送达 1% 的截断形态。
name: big_prompt
description: 截断哨兵夹具

nodes:
  - id: solo
    type: llm
    model: Fake:primary
    fallback: [Fake:fallback]
    prompt: 处理下方材料并给出结论。
    consumes: [task]

edges:
  - from: solo
    to: END
